import { Grid } from '@material-ui/core';
import React from 'react';
import Content from './Content';

const Home = () => {
    return (
        <div>
            <Grid>
                <Content/>
            </Grid>
        </div>
    );
};

export default Home;